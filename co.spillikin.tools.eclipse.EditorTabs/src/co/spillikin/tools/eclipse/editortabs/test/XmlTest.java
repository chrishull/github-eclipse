package co.spillikin.tools.eclipse.editortabs.test;

import static org.junit.Assert.*;

import java.util.Arrays;
import java.util.List;
import java.util.Map;

import org.junit.Test;

import co.spillikin.tools.eclipse.editortabs.model.EditorSessionsData;
import co.spillikin.tools.eclipse.editortabs.model.EditorSession;
import co.spillikin.tools.eclipse.editortabs.model.EditorSessionsData;
import co.spillikin.tools.eclipse.editortabs.model.SessionMap;
import co.spillikin.tools.eclipse.editortabs.util.Builder;

public class XmlTest {

    @Test
    public void test() {
        // fail("Not yet implemented");
        
        /*
        EditorSessionData wm = new EditorSessionsDataImpl();
        SessionMap sm = wm.getSessionMap("Test-Workspace1");
        EditorSessionImpl es = sm.switchEditorSession("Test-SessionName1-ws1");
        String[] paths = {"path1", "path2", "path3"};
        es.setFilePathList ( Arrays.asList(paths) );
        es = sm.switchEditorSession("Test-SessionName2-ws1");
        String[] paths2 = {"path1-2", "path2-2", "path3-2"};
        es.setFilePathList ( Arrays.asList(paths2) );        
        
        sm = wm.getSessionMap("Test-Workspace2");
        es = sm.switchEditorSession("Test-SessionName1-ws2");
        String[] paths3 = {"path1-ws2", "path2-ws2", "path3-ws2"};
        es.setFilePathList ( Arrays.asList(paths3) );
        */
        
        // Builder.save(wm);
        
    }

}
